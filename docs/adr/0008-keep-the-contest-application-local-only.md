# Keep the contest application local only

The MoonIPFIX contest application will live only under the ignored local `.private/` directory. It is excluded from Git history, CI, release archives, and Mooncakes publication so participant/contact information and the application document never enter the public repository.
